<h2>View a cart</h2>

Contains:
<ul>
<% @cart.items.each do |i| %>
  <li><%= i.name %></li>
<% end %>
</ul>

<div>
  Subtotal: $<%= @cart.subtotal %><br/>
  Total: $<%= @cart.total %>
</div>

<div>
<h4>Available Incentives</h4>

<ul>
  <% @cart.incentive_campaigns.each do |ic| %>
    <li><%= ic.name %></li>
  <% end %>
</ul>
